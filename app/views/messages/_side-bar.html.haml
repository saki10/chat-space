.side
  .side-header
    .top-items
      .name
        .user
          = current_user.name
        .icon
          = link_to '#' do
            %i.far.fa-edit
          = link_to edit_user_path(current_user) do
            %i.fas.fa-cog


  .groups
    .group
      .group__name
        test
      .group__message
        メッセージなし